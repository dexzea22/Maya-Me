<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error</title>
  <!-- Include your stylesheet here if you have one -->
</head>
<body>
  <h1>Oops! Something went wrong...</h1>
  <!-- Display the error message -->
  <% if (error) { %>
    <div class="error-message">
      <p><%= error.message ? error.message : "An unexpected error occurred." %></p>
    </div>
  <% } else { %>
    <div class="error-message">
      <p>An unexpected error occurred and no further information is available.</p>
    </div>
  <% } %>

  <!-- You can include a link back to the homepage or dashboard -->
  <a href="/">Go back to the homepage</a>

  <script>
    // Here you could include some JavaScript to handle the error, if needed.
  </script>
</body>
</html>
