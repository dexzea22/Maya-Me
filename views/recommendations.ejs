<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dietary Recommendations</title>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    />
    <!-- Add any additional head content (like CSS links) here -->
  </head>
  <body>
    <div class="container mt-4">
      <h1 class="display-4 text-center">Your Dietary Recommendations</h1>
      <!-- Recommendations container where the list of recommendations will be displayed -->
      <div id="recommendations-container" class="mt-3">
        <!-- Check if there are any recommendations to display -->
        <% if(recommendations && recommendations.length > 0) { %>
        <ul class="list-group">
          <!-- Iterate over each recommendation and display it as a list item -->
          <% recommendations.forEach(function(rec, index) { %>
          <li class="list-group-item"><%= rec %></li>
          <% }); %>
        </ul>
        <% } else { %>
        <!-- Message displayed if there are no recommendations -->
        <div class="alert alert-info" role="alert">
          No recommendations available. Please adjust your dietary preferences.
        </div>
        <% } %>
      </div>
      <!-- Link to navigate back to the user dashboard -->
      <a href="/UserDashboard" class="btn btn-primary mt-3"
        >Back to Dashboard</a
      >
    </div>
    <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  </body>
</html>
